

    function add(x, y){
    var result = x + y;
    return result;
    }

    function sub(x, y) {
    return x - y;
    }

    function mult(x, y) {
    return x * y;
    }

    function div(x, y) {
    return x / y;
    }

    function pow(x) {
        return x*x;
    }

    function sqrt(x) {
        return Math.sqrt(x);
    }
   

    function printResult() {
        console.log (add(2, 2));
        console.log (sub(5, 3));
        console.log (mult(5, 3));
        console.log (div(9, 3));
        console.log (pow(4));
        console.log (sqrt(16));
    }

    console.log(printResult());




    


